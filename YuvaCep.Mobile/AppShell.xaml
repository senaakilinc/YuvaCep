<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="YuvaCep.Mobile.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:YuvaCep.Mobile"
    xmlns:views="clr-namespace:YuvaCep.Mobile.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="YuvaCep.Mobile">

    <ShellContent
        Title="Giriş"
        ContentTemplate="{DataTemplate views:RoleSelectionPage}"
        Route="RoleSelectionPage"
        Shell.TabBarIsVisible="False" />

    <ShellContent 
        ContentTemplate="{DataTemplate views:TeacherHomePage}" 
        Route="TeacherHomePage" />

    <ShellContent 
        ContentTemplate="{DataTemplate views:ParentHomePage}" 
        Route="ParentHomePage" />

</Shell>