<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:YuvaCep.Mobile.ViewModels"
             x:Class="YuvaCep.Mobile.Views.StudentCardsPage"
             x:DataType="viewModels:StudentCardsViewModel"
             Title="Öğrenci Detayı"
             Shell.NavBarIsVisible="False"
             BackgroundColor="#F5F5F5">

    <Grid RowDefinitions="Auto, *">

        <Grid Grid.Row="0" BackgroundColor="#4A90E2" Padding="20,40,20,30" RowDefinitions="Auto, Auto">
            <Grid ColumnDefinitions="Auto, *" VerticalOptions="Center">
                <ImageButton Command="{Binding GoBackCommand}" HeightRequest="24" HorizontalOptions="Start" BackgroundColor="Transparent">
                    <ImageButton.Source>
                        <FontImageSource Glyph="&#x2190;" Color="White" Size="24" />
                    </ImageButton.Source>
                </ImageButton>

                <Label Text="{Binding ClassName}" 
                       TextColor="White" FontSize="22" FontAttributes="Bold" 
                       HorizontalOptions="Center" VerticalOptions="Center" 
                       Grid.Column="1" Margin="-24,0,0,0"/>
            </Grid>

            <StackLayout Grid.Row="1" Margin="0,20,0,0">
                <Label Text="Öğrenci:" TextColor="#E0E0E0" FontSize="14"/>
                <Label Text="{Binding StudentName}" TextColor="White" FontSize="28" FontAttributes="Bold"/>
            </StackLayout>
        </Grid>

        <ScrollView Grid.Row="1" Padding="20">
            <VerticalStackLayout Spacing="20">

                <Button Text="➕ GÜNLÜK RAPOR EKLE"
                        Command="{Binding GoToTeacherDailyReportCommand}"
                        IsVisible="{Binding IsTeacher}"
                        BackgroundColor="#FF7043"
                        TextColor="White"
                        FontAttributes="Bold"
                        CornerRadius="15"
                        HeightRequest="60"
                        ImageSource="{FontImageSource Glyph='📝', Color=White, Size=20}"
                        ContentLayout="Right,10"/>

                <Grid ColumnDefinitions="*,*" RowDefinitions="140, 140, 140" ColumnSpacing="15" RowSpacing="15">

                    <Frame Grid.Row="0" Grid.Column="0" CornerRadius="15" HasShadow="True" Padding="0" BorderColor="Transparent">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding GoToDailyReportCommand}"/>
                        </Frame.GestureRecognizers>
                        <StackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="10">
                            <Label Text="📝" FontSize="40" HorizontalOptions="Center"/>
                            <Label Text="Günlük Rapor" FontAttributes="Bold" TextColor="#333"/>
                        </StackLayout>
                    </Frame>

                    <Frame Grid.Row="0" Grid.Column="1" CornerRadius="15" HasShadow="True" Padding="0" BorderColor="Transparent">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding GoToAnnouncementsCommand}"/>
                        </Frame.GestureRecognizers>
                        <StackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="10">
                            <Label Text="📢" FontSize="40" HorizontalOptions="Center"/>
                            <Label Text="Duyurular" FontAttributes="Bold" TextColor="#333"/>
                        </StackLayout>
                    </Frame>

                    <Frame Grid.Row="1" Grid.Column="0" CornerRadius="15" HasShadow="True" Padding="0" BorderColor="Transparent">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding GoToFoodListCommand}"/>
                        </Frame.GestureRecognizers>
                        <StackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="10">
                            <Label Text="🍎" FontSize="40" HorizontalOptions="Center"/>
                            <Label Text="Yemek Listesi" FontAttributes="Bold" TextColor="#333"/>
                        </StackLayout>
                    </Frame>

                    <Frame Grid.Row="1" Grid.Column="1" CornerRadius="15" HasShadow="True" Padding="0" BorderColor="Transparent">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding GoToLessonProgramCommand}"/>
                        </Frame.GestureRecognizers>
                        <StackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="10">
                            <Label Text="📅" FontSize="40" HorizontalOptions="Center"/>
                            <Label Text="Ders Programı" FontAttributes="Bold" TextColor="#333"/>
                        </StackLayout>
                    </Frame>

                    <Frame Grid.Row="2" Grid.Column="0" CornerRadius="15" HasShadow="True" Padding="0" BorderColor="Transparent">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding GoToBadgeDetailsCommand}"/>
                        </Frame.GestureRecognizers>
                        <StackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="10">
                            <Label Text="🏆" FontSize="40" HorizontalOptions="Center"/>
                            <Label Text="Rozetlerim" FontAttributes="Bold" TextColor="#333"/>
                        </StackLayout>
                    </Frame>

                    <Frame Grid.Row="2" Grid.Column="1" CornerRadius="15" HasShadow="True" Padding="0" BackgroundColor="#EEE" BorderColor="Transparent">
                        <StackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="10">
                            <Label Text="⚙️" FontSize="40" HorizontalOptions="Center"/>
                            <Label Text="Ayarlar" FontAttributes="Bold" TextColor="Gray"/>
                        </StackLayout>
                    </Frame>

                </Grid>
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>