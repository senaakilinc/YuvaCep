<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:YuvaCep.Mobile.ViewModels"
             x:Class="YuvaCep.Mobile.Views.BadgeTrackingPage"
             Title="Rozetlerim"
             BackgroundColor="#F3F4F6"
             Shell.NavBarIsVisible="False">

    <ContentPage.BindingContext>
        <vm:TeacherBadgeStatusViewModel />
    </ContentPage.BindingContext>

    <Grid RowDefinitions="Auto, Auto, *">
        <Frame Grid.Row="0" BackgroundColor="#3B82F6" CornerRadius="0" Padding="20,40,20,20" HasShadow="False">
            <Grid ColumnDefinitions="Auto, *">
                <ImageButton Command="{Binding GoBackCommand}" BackgroundColor="Transparent" VerticalOptions="Center" HorizontalOptions="Start">
                    <ImageButton.Source>
                        <FontImageSource Glyph="&#x2190;" Color="White" Size="24"/>
                    </ImageButton.Source>
                </ImageButton>
                <Label Grid.Column="1" Text="Sınıf Rozet Durumu" TextColor="White" FontSize="20" FontAttributes="Bold" HorizontalOptions="Center" VerticalOptions="Center" Margin="-40,0,0,0"/>
            </Grid>
        </Frame>

        <Label Grid.Row="1" 
           Text="{Binding CurrentMonthYear}" 
           FontSize="18" 
           FontAttributes="Bold" 
           TextColor="#1F2937" 
           HorizontalOptions="Center" 
           Margin="0,15,0,5"/>

        <CollectionView Grid.Row="2" ItemsSource="{Binding Students}">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="vm:ClassBadgeStatusItem">
                    <Frame Margin="15,5,15,5" Padding="15" CornerRadius="12" HasShadow="False" BackgroundColor="White" BorderColor="#E5E7EB">
                        <Grid ColumnDefinitions="*, Auto">
                            <VerticalStackLayout Grid.Column="0" VerticalOptions="Center">
                                <Label Text="{Binding Name}" FontAttributes="Bold" FontSize="16" TextColor="#1F2937"/>
                                <Label Text="Öğrenci" FontSize="11" TextColor="Gray"/>
                            </VerticalStackLayout>
                            <HorizontalStackLayout Grid.Column="1" Spacing="15" VerticalOptions="Center">
                                <Image Source="{Binding BadgeIcon}" 
                                   HeightRequest="40" 
                                   WidthRequest="40" 
                                   Aspect="AspectFit" 
                                   VerticalOptions="Center"/>
                                <Frame Padding="10,5" CornerRadius="8" BackgroundColor="#FFF7ED" HasShadow="False" BorderColor="#FFEDD5">
                                    <HorizontalStackLayout Spacing="2" VerticalOptions="Center">
                                        <Label Text="{Binding CompletedDays}" FontAttributes="Bold" TextColor="#C2410C" FontSize="14"/>
                                        <Label Text="/ 30" TextColor="Gray" FontSize="14"/>
                                    </HorizontalStackLayout>
                                </Frame>
                            </HorizontalStackLayout>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>