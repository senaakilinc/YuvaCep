<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:YuvaCep.Mobile.ViewModels"
             x:Class="YuvaCep.Mobile.Views.BadgeTrackingPage"
             Title="Rozet Takibi"
             BackgroundColor="#F3F4F6"
             Shell.NavBarIsVisible="False">

    <ContentPage.BindingContext>
        <vm:BadgeTrackingViewModel />
    </ContentPage.BindingContext>

    <Grid RowDefinitions="Auto, *">

        <Frame Grid.Row="0" BackgroundColor="#F59E0B" CornerRadius="0" Padding="20,40,20,20" HasShadow="False">
            <Grid ColumnDefinitions="Auto, *">
                <Button Text="Geri" Command="{Binding GoBackCommand}" TextColor="White" BackgroundColor="Transparent" HorizontalOptions="Start"/>
                <Label Grid.Column="1" Text="Sınıf Rozet Durumu" TextColor="White" FontSize="20" FontAttributes="Bold" HorizontalOptions="Center" VerticalOptions="Center" Margin="-60,0,0,0"/>
            </Grid>
        </Frame>

        <CollectionView Grid.Row="1" ItemsSource="{Binding Students}" Margin="0,10,0,0">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="15,5,15,5" Padding="15" CornerRadius="12" HasShadow="False" BackgroundColor="White" BorderColor="#E5E7EB">
                        <Grid ColumnDefinitions="Auto, *, Auto">
                            
                            <VerticalStackLayout Grid.Column="0" VerticalOptions="Center">
                                <Label Text="{Binding Name}" FontAttributes="Bold" FontSize="16" TextColor="#1F2937"/>
                                <Label Text="Papatyalar Sınıfı" FontSize="11" TextColor="Gray"/>
                            </VerticalStackLayout>

                            <HorizontalStackLayout Grid.Column="2" Spacing="15" VerticalOptions="Center">
                                
                                <Label Text="{Binding BadgeIcon}" 
                                       FontSize="32" 
                                       VerticalOptions="Center"
                                       BackgroundColor="Transparent"
                                       Opacity="{Binding HasBadge, Converter={StaticResource BoolToOpacityConverter}}"/>

                                <Frame Padding="10,5" CornerRadius="8" BackgroundColor="#FFF7ED" HasShadow="False" BorderColor="#FFEDD5">
                                    <HorizontalStackLayout Spacing="2" VerticalOptions="Center">
                                        <Label Text="{Binding CompletedDays}" FontAttributes="Bold" TextColor="#C2410C" FontSize="14"/>
                                        <Label Text="/ 30" TextColor="Gray" FontSize="14"/>
                                    </HorizontalStackLayout>
                                </Frame>

                            </HorizontalStackLayout>

                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </Grid>
</ContentPage>