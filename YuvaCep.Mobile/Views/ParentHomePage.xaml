<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:YuvaCep.Mobile.ViewModels"
             x:Class="YuvaCep.Mobile.Views.ParentHomePage"
             Title="Veli Ana Sayfası"
             BackgroundColor="#F5F5F5"
             Shell.NavBarIsVisible="False">

    <Grid RowDefinitions="Auto, *" Padding="0">

        <Grid Grid.Row="0" BackgroundColor="#4A90E2" Padding="20,40,20,30" ColumnDefinitions="*, Auto">

            <VerticalStackLayout Grid.Column="0" Spacing="2">
                <Label Text="Hoşgeldiniz," 
                       TextColor="#E3F2FD" FontSize="14"/>

                <Label Text="{Binding ParentName}" 
                       TextColor="White" FontSize="20" FontAttributes="Bold"/>
            </VerticalStackLayout>

            <ImageButton Grid.Column="1" 
                         Command="{Binding LogoutCommand}" 
                         HeightRequest="30" 
                         VerticalOptions="Start">
                <ImageButton.Source>
                    <FontImageSource Glyph="🚪" Color="White" Size="30"/>
                </ImageButton.Source>
            </ImageButton>
        </Grid>

        <ScrollView Grid.Row="1" Padding="20">
            <VerticalStackLayout Spacing="20">

                <ActivityIndicator IsRunning="{Binding IsBusy}" 
                                   IsVisible="{Binding IsBusy}" 
                                   Color="#4A90E2"
                                   HeightRequest="40"/>

                <Frame IsVisible="{Binding HasStudent}" 
                       BackgroundColor="White" CornerRadius="15" Padding="20" HasShadow="True" BorderColor="Transparent">
                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding GoToStudentDetailCommand}" />
                    </Frame.GestureRecognizers>

                    <VerticalStackLayout Spacing="15">
                        <Label Text="🎓 Öğrenci Detayları" FontSize="16" TextColor="#4A90E2" FontAttributes="Bold" HorizontalOptions="Center"/>
                        <BoxView HeightRequest="1" Color="#F0F0F0" />

                        <VerticalStackLayout Spacing="5" HorizontalOptions="Center">
                            <Label Text="{Binding Student.FullName}" 
                                    FontSize="24" FontAttributes="Bold" TextColor="#2D3E50" HorizontalOptions="Center"/>

                            <Label Text="{Binding Student.ClassName}" 
                                    FontSize="16" TextColor="Orange" HorizontalOptions="Center"/>
                        </VerticalStackLayout>

                        <Label Text="Detaylar için tıklayınız 👉" FontSize="12" TextColor="Gray" HorizontalOptions="Center"/>
                    </VerticalStackLayout>
                </Frame>

                <VerticalStackLayout Spacing="15">
                    <VerticalStackLayout.Triggers>
                        <DataTrigger TargetType="VerticalStackLayout" Binding="{Binding HasStudent}" Value="True">
                            <Setter Property="IsVisible" Value="False" />
                        </DataTrigger>
                    </VerticalStackLayout.Triggers>

                    <Label Text="🚫" FontSize="60" HorizontalOptions="Center"/>
                    <Label Text="Henüz Bir Çocuk Eklemediniz" 
                           FontSize="18" FontAttributes="Bold" TextColor="#E53935" HorizontalOptions="Center"/>
                    <Label Text="Kurum kodunuzla çocuğunuzu ekleyebilirsiniz." 
                           FontSize="14" TextColor="Gray" HorizontalOptions="Center" HorizontalTextAlignment="Center"/>
                </VerticalStackLayout>

                <BoxView HeightRequest="1" Color="#E0E0E0" Margin="0,10"/>

                <Button Text="➕ Çocuk Ekle" 
                        Command="{Binding AddChildCommand}" 
                        BackgroundColor="#2D3E50" 
                        TextColor="White" 
                        FontAttributes="Bold"
                        CornerRadius="25"
                        HeightRequest="50"/>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>

</ContentPage>