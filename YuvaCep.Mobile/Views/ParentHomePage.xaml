<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:YuvaCep.Mobile.ViewModels"
             x:Class="YuvaCep.Mobile.Views.ParentHomePage"
             Title="Çocuklarım"
             BackgroundColor="#F3F4F6">
    <ContentPage.BindingContext>
        <vm:ParentHomeViewModel />
    </ContentPage.BindingContext>

    <Grid RowDefinitions="*, Auto">

        <CollectionView Grid.Row="0" ItemsSource="{Binding MyChildren}" Margin="20">
            <CollectionView.EmptyView>
                <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="10">
                    <Label Text="🚸" FontSize="50" HorizontalOptions="Center"/>
                    <Label Text="Henüz kayıtlı çocuğunuz yok." TextColor="Gray" HorizontalOptions="Center"/>
                    <Label Text="Referans kodu ile ekleme yapın." TextColor="Gray" FontSize="12" HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </CollectionView.EmptyView>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="0,10" Padding="0" CornerRadius="15" HasShadow="True" BorderColor="Transparent">
                        <Grid ColumnDefinitions="100, *" BackgroundColor="White">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ParentHomeViewModel}}, Path=OpenChildReportCommand}" CommandParameter="{Binding .}"/>
                            </Grid.GestureRecognizers>

                            <BoxView Grid.Column="0" Color="#E0F2FE"/>
                            <Label Grid.Column="0" Text="👶" FontSize="40" VerticalOptions="Center" HorizontalOptions="Center"/>

                            <VerticalStackLayout Grid.Column="1" Padding="15" Spacing="5" VerticalOptions="Center">
                                <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" TextColor="#1F2937"/>
                                <Label Text="{Binding ClassName}" FontSize="14" TextColor="Gray"/>
                                <Label Text="Raporu Görüntüle >" FontSize="12" TextColor="#2563EB" Margin="0,5,0,0"/>
                            </VerticalStackLayout>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Grid.Row="1"
                Text="+ ÇOCUK EKLE"
                Command="{Binding AddChildByCodeCommand}"
                BackgroundColor="#2563EB"
                TextColor="White"
                FontAttributes="Bold"
                CornerRadius="25"
                HeightRequest="60"
                Margin="30,10,30,30"
                Shadow="{Shadow Brush=Black, Offset='0,5', Opacity=0.3}"/>

    </Grid>
</ContentPage>