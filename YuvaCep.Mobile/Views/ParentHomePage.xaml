<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:YuvaCep.Mobile.ViewModels"
             xmlns:dtos="clr-namespace:YuvaCep.Mobile.Dtos"
             x:Class="YuvaCep.Mobile.Views.ParentHomePage"
             x:DataType="vm:ParentHomeViewModel"
             Title="Veli Paneli"
             BackgroundColor="#F3F4F6"
             Shell.NavBarIsVisible="False">

    <Grid RowDefinitions="Auto, *">
        <Frame Grid.Row="0" BackgroundColor="#2563EB" CornerRadius="0" Padding="20,40,20,20" HasShadow="False">
            <Grid ColumnDefinitions="*, Auto">
                <VerticalStackLayout Grid.Column="0">
                    <Label Text="{Binding ParentName, StringFormat='Merhaba, {0}'}" TextColor="White" Opacity="0.9"/>
                    <Label Text="Çocuklarım" TextColor="White" FontSize="24" FontAttributes="Bold"/>
                </VerticalStackLayout>
                <ImageButton Grid.Column="1" Source="{FontImage Glyph='🚪', Color='White', Size=28}"
                             BackgroundColor="Transparent" Command="{Binding LogoutCommand}" HeightRequest="40"/>
            </Grid>
        </Frame>

        <ScrollView Grid.Row="1" Padding="20">
            <VerticalStackLayout Spacing="20">

                <Frame BackgroundColor="White" CornerRadius="15" HasShadow="True" Padding="20">
                    <VerticalStackLayout Spacing="15">
                        <Label Text="🔗 Öğrenci Eşleştir" FontAttributes="Bold" FontSize="16" TextColor="#374151"/>
                        <Label Text="Öğretmeninizden aldığınız referans kodunu giriniz:" FontSize="12" TextColor="Gray"/>

                        <Frame BorderColor="#E5E7EB" Padding="0" CornerRadius="10" HasShadow="False">
                            <Entry Placeholder="Örn: A7X29B" 
                                   Text="{Binding ReferenceCodeInput}"
                                   HorizontalTextAlignment="Center"
                                   FontSize="18"
                                   IsTextPredictionEnabled="False"
                                   MaxLength="6"
                                   HeightRequest="50"/>
                        </Frame>

                        <Button Text="Eşleştir ve Ekle" 
                                Command="{Binding LinkStudentCommand}"
                                BackgroundColor="#2563EB" 
                                TextColor="White" 
                                CornerRadius="10" 
                                FontAttributes="Bold"/>
                    </VerticalStackLayout>
                </Frame>

                <Label Text="Kayıtlı Öğrenciler" FontAttributes="Bold" TextColor="#374151" Margin="5,10,0,0"/>

                <CollectionView ItemsSource="{Binding MyChildren}">
                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout Orientation="Vertical" ItemSpacing="10"/>
                    </CollectionView.ItemsLayout>
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="dtos:ChildDto">
                            <Frame CornerRadius="15" HasShadow="True" Padding="15" BackgroundColor="White" BorderColor="Transparent">

                                <Frame.GestureRecognizers>
                                    <TapGestureRecognizer 
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ParentHomeViewModel}}, Path=GoToChildCardsCommand}"
                                        CommandParameter="{Binding .}" />
                                </Frame.GestureRecognizers>

                                <Grid ColumnDefinitions="Auto, *, Auto">
                                    <Frame Grid.Column="0" HeightRequest="50" WidthRequest="50" CornerRadius="25" BackgroundColor="#DBEAFE" Padding="0" HorizontalOptions="Start" HasShadow="False">
                                        <Label Text="👶" FontSize="24" HorizontalOptions="Center" VerticalOptions="Center"/>
                                    </Frame>

                                    <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Margin="15,0,0,0">
                                        <Label Text="{Binding Name}" FontAttributes="Bold" FontSize="16" TextColor="#1E3A8A"/>
                                        <Label Text="{Binding ClassName}" FontSize="12" TextColor="Gray"/>
                                    </VerticalStackLayout>

                                    <Label Grid.Column="2" Text="✅ Bağlı" TextColor="#22C55E" FontSize="12" VerticalOptions="Center" FontAttributes="Bold"/>
                                </Grid>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                    <CollectionView.EmptyView>
                        <Label Text="Henüz ekli öğrenci yok." HorizontalOptions="Center" TextColor="Gray" Margin="0,20,0,0"/>
                    </CollectionView.EmptyView>
                </CollectionView>

            </VerticalStackLayout>
        </ScrollView>

        <ActivityIndicator Grid.RowSpan="2" IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" 
                           Color="#2563EB" HorizontalOptions="Center" VerticalOptions="Center"/>
    </Grid>
</ContentPage>