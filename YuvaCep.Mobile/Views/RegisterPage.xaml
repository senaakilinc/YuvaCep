<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:YuvaCep.Mobile.ViewModels"
             x:Class="YuvaCep.Mobile.Views.RegisterPage"
             Title="Öğretmen Kaydı">

    <ContentPage.BindingContext>
        <vm:RegisterViewModel />
    </ContentPage.BindingContext>

    <ScrollView>
        <VerticalStackLayout Spacing="15" Padding="30" VerticalOptions="Center">

            <Label Text="Yeni Hesap Oluştur" 
                   FontSize="24" 
                   FontAttributes="Bold" 
                   HorizontalOptions="Center" />

            <Entry Placeholder="Ad" Text="{Binding Name}" />
            <Entry Placeholder="Soyad" Text="{Binding Surname}" />
            <Entry Placeholder="TC Kimlik No" Text="{Binding TcIdNumber}" Keyboard="Numeric" />
            <Entry Placeholder="Telefon Numarası" Text="{Binding PhoneNumber}" Keyboard="Telephone" />
            <Entry Placeholder="Şifre Belirle" IsPassword="True" Text="{Binding Password}" />

            <Button Text="Kayıt Ol ve Devam Et" 
                    Command="{Binding RegisterCommand}"
                    BackgroundColor="#2196F3" 
                    TextColor="White"
                    Margin="0,20,0,0"/>

            <Button Text="Giriş Ekranına Dön" 
                    Command="{Binding GoBackCommand}"
                    BackgroundColor="Transparent" 
                    TextColor="Gray" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>