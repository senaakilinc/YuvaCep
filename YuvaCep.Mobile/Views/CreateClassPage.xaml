<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:YuvaCep.Mobile.ViewModels"
             x:Class="YuvaCep.Mobile.Views.CreateClassPage"
             Title="Sınıf Oluştur"
             Shell.NavBarIsVisible="False"
             BackgroundColor="#F5F5F5">

    <ScrollView>
        <VerticalStackLayout Spacing="20" Padding="30" VerticalOptions="Center">

            <Label Text="🏫" FontSize="60" HorizontalOptions="Center"/>

            <Label Text="Sınıfınızı Oluşturun" 
                   FontSize="24" 
                   FontAttributes="Bold" 
                   TextColor="#2D3E50" 
                   HorizontalOptions="Center"/>

            <Label Text="Lütfen oluşturacağınız sınıfın bilgilerini giriniz." 
                   TextColor="Gray" HorizontalOptions="Center" HorizontalTextAlignment="Center"/>

            <Frame CornerRadius="10" Padding="0" HasShadow="False" BorderColor="Gray">
                <Entry Placeholder="Sınıf Adı (Örn: Papatyalar)" 
                       Text="{Binding ClassName}"
                       HeightRequest="50" Margin="10,0"/>
            </Frame>

            <Frame CornerRadius="10" Padding="0" HasShadow="False" BorderColor="Gray">
                <Picker Title="Yaş Grubu Seçiniz"
                        ItemsSource="{Binding AgeGroups}"
                        SelectedItem="{Binding SelectedAgeGroup}"
                        HeightRequest="50" Margin="10,0"/>
            </Frame>

            <Button Text="Sınıfı Oluştur ve Başla" 
                    Command="{Binding CreateClassCommand}"
                    BackgroundColor="#2D3E50" 
                    TextColor="White"
                    FontAttributes="Bold"
                    CornerRadius="25"
                    HeightRequest="50"
                    Margin="0,20,0,0"/>

            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" Color="#2D3E50"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>