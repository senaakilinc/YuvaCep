<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:YuvaCep.Mobile.ViewModels"
             x:Class="YuvaCep.Mobile.Views.ParentDailyReportPage"
             Title="Günlük Rapor"
             BackgroundColor="#F3F4F6"
             Shell.NavBarIsVisible="False">

    <ContentPage.BindingContext>
        <vm:ParentDailyReportViewModel />
    </ContentPage.BindingContext>

    <Grid RowDefinitions="Auto, *">

        <Frame Grid.Row="0" BackgroundColor="#2563EB" CornerRadius="0" Padding="20,40,20,20" HasShadow="False">
            <Grid ColumnDefinitions="Auto, *">
                <Button Grid.Column="0" Text="Geri" Command="{Binding GoBackCommand}" TextColor="White" BackgroundColor="Transparent" HorizontalOptions="Start" Margin="-10,0,0,0"/>
                <Label Grid.Column="1" Text="Günlük Rapor" TextColor="White" FontSize="20" FontAttributes="Bold" HorizontalOptions="Center" Margin="-40,0,0,0"/>
            </Grid>
        </Frame>

        <ScrollView Grid.Row="1" Padding="20">
            <VerticalStackLayout Spacing="20">

                <Label Text="{Binding Date}" HorizontalOptions="Center" TextColor="Gray" FontSize="14"/>

                <Frame BackgroundColor="White" CornerRadius="15" BorderColor="Transparent" HasShadow="True" Padding="20">
                    <VerticalStackLayout Spacing="10" HorizontalOptions="Center">
                        <Label Text="Günü Nasıl Geçti?" TextColor="Gray" FontSize="12" HorizontalOptions="Center"/>
                        <Label Text="{Binding MoodEmoji}" FontSize="60" HorizontalOptions="Center"/>
                        <Label Text="{Binding MoodTitle}" TextColor="#2563EB" FontSize="24" FontAttributes="Bold" HorizontalOptions="Center"/>
                    </VerticalStackLayout>
                </Frame>

                <Frame BackgroundColor="White" CornerRadius="15" BorderColor="Transparent" HasShadow="True" Padding="15">
                    <VerticalStackLayout Spacing="8">
                        <Label Text="🍽️ Beslenme" TextColor="#1F2937" FontAttributes="Bold" FontSize="16"/>
                        <BoxView HeightRequest="1" Color="#F3F4F6"/>
                        <Label Text="{Binding NutritionInfo}" TextColor="#4B5563" FontSize="14"/>
                    </VerticalStackLayout>
                </Frame>

                <Frame BackgroundColor="White" CornerRadius="15" BorderColor="Transparent" HasShadow="True" Padding="15">
                    <VerticalStackLayout Spacing="8">
                        <Label Text="📝 Öğretmenden Not" TextColor="#1F2937" FontAttributes="Bold" FontSize="16"/>
                        <BoxView HeightRequest="1" Color="#F3F4F6"/>
                        <Label Text="{Binding TeacherNote}" TextColor="#4B5563" FontSize="14" FontAttributes="Italic"/>
                    </VerticalStackLayout>
                </Frame>

            </VerticalStackLayout>
        </ScrollView>

    </Grid>
</ContentPage>