<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:YuvaCep.Mobile.ViewModels"
             x:Class="YuvaCep.Mobile.Views.AnnouncementsPage"
             Title="Duyurular"
             BackgroundColor="#F5F5F5"
             Shell.NavBarIsVisible="False">

    <Grid RowDefinitions="Auto, *">

        <Grid Grid.Row="0" BackgroundColor="#4A90E2" Padding="15" ColumnDefinitions="Auto, *">
            <ImageButton Command="{Binding GoBackCommand}" HeightRequest="24" HorizontalOptions="Start">
                <ImageButton.Source>
                    <FontImageSource Glyph="&#x2190;" Color="White" Size="24" />
                </ImageButton.Source>
            </ImageButton>

            <Label Grid.Column="1" Text="Sınıf Duyuruları" 
                   TextColor="White" FontSize="20" FontAttributes="Bold" 
                   HorizontalOptions="Center" VerticalOptions="Center" Margin="-24,0,0,0"/>
        </Grid>

        <ActivityIndicator Grid.Row="1" IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" 
                           Color="#4A90E2" HorizontalOptions="Center" VerticalOptions="Center"/>

        <CollectionView Grid.Row="1" ItemsSource="{Binding Announcements}" Margin="0,10,0,0">

            <CollectionView.EmptyView>
                <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
                    <Label Text="📭" FontSize="50" HorizontalOptions="Center"/>
                    <Label Text="Henüz bir duyuru yok." TextColor="Gray" HorizontalOptions="Center"/>
                </StackLayout>
            </CollectionView.EmptyView>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="15,5" Padding="15" CornerRadius="10" HasShadow="True" BorderColor="Transparent" BackgroundColor="White">
                        <VerticalStackLayout Spacing="8">

                            <Grid ColumnDefinitions="*, Auto">
                                <Label Grid.Column="0" Text="{Binding Title}" FontAttributes="Bold" TextColor="#2D3E50" FontSize="16"/>
                                <Label Grid.Column="1" Text="{Binding CreatedDate, StringFormat='{0:dd.MM.yyyy}'}" TextColor="Gray" FontSize="12"/>
                            </Grid>

                            <Label Text="{Binding Content}" TextColor="#555" FontSize="14"/>

                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </Grid>
</ContentPage>